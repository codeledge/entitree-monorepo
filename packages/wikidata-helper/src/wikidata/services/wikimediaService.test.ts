import searchImage from "./wikimediaService";

describe("searchImage", () => {
  test("it should give the correct name", async () => {
    // const res = await searchImage("Akaram dada pawar");
    // console.log("run image");

    // console.log(res);

    expect("enrique-iglesias").toBe("enrique-iglesias");
  });
});
